# İNNO Gayrimenkul Lead Reporter - Environment Variables

# Development Configuration
NODE_ENV=development

# Authentication Configuration
# IMPORTANT: Change these credentials before deployment!
ADMIN_USERNAME=admin
ADMIN_PASSWORD=A.yaman*secure123!LeadTracker

# Session Security
# IMPORTANT: Generate a strong, random session secret for production!
SESSION_SECRET=your-super-secret-session-key-change-this-in-production-make-it-long-and-random

# Optional: Database URL (not required for local memory storage)
# DATABASE_URL=postgresql://username:password@localhost:5432/inno_leads

# Optional: Storage Type (memory|database)
STORAGE_TYPE=memory

# Optional: Port Configuration (default: 5000)
# PORT=5000

# Optional: API Keys (only needed for specific integrations)
# ANTHROPIC_API_KEY=your_anthropic_key_here

# Local Development Notes:
# - All variables above are OPTIONAL for basic functionality
# - The app works completely with in-memory storage
# - MUST change ADMIN_USERNAME, ADMIN_PASSWORD, and SESSION_SECRET before deployment!
# - Database URL only needed if using PostgreSQL
# - Copy this file to .env to override defaults